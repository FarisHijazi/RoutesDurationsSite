---
description: 
globs: 
alwaysApply: true
---
# Active Context: Google Maps Routing Distance Matrix Calculator

## Current Work Focus
The current focus is on implementing major new features now that the core calculation and visualization logic is stable. The immediate next step is building the "Find Closest Facilities" feature.

## Recent Changes
*   **Implemented Color Consistency**: A major UI enhancement where each location is assigned a unique, persistent color. This color is now used consistently across the location list, map pins (for both selection and results), and the results charts and summary, providing a much clearer and more intuitive user experience.
*   **Improved Chart Visibility**: The confidence band plots on the results page have been adjusted to be semi-transparent, which prevents overlapping plots from obscuring each other. The opacity was tuned to ensure the bands remain clearly visible.
*   **Fixed Bouncing Pin Color**: The temporary "bouncing" pin on the selection map now correctly shows the color that will be assigned to the next location, providing immediate visual feedback to the user.
*   **Fixed Calculation Accuracy**: Addressed a major bug where travel time calculations were incorrect. The app no longer uses mock data or arbitrary "multipliers." All calculations for best, worst, and average times are now based on the correct `optimistic`, `pessimistic`, and `best_guess` models from the Google Maps API.
*   **Implemented Return Trips**: Added a major new feature to calculate and display return trips. The app now fetches data for both directions (e.g., Home to Work and Work to Home) and displays them as separate items in the summary and as distinct confidence bands in the charts.
*   **Fixed Critical API Bug**: Resolved the `departure_time is in the past` error by ensuring the departure time for Google Maps API calls is always set to a future date. This unblocked the core calculation functionality.
*   **Fixed Results Summary Calculation**: The summary now correctly calculates average time from `best_guess`, best time from `optimistic`, and worst time from `pessimistic` results, providing a more accurate and intuitive summary.
*   **Implemented "Find Closest Facilities"**: Users can now define types of facilities (e.g., gyms, minimarkets), and the app will find and add the nearest ones to the map based on the property's location.
*   **Added Loading Indicators**: Implemented a loading state across the results page to provide clear feedback to the user while calculations are running.
*   **Added Print Functionality**: Users can now print a clean, well-formatted report of the results page.
*   **Enhanced Location Input**: The location name field can be submitted with the Enter key, and the location name is correctly pre-filled from search results without being overwritten by reverse geocoding.

## Next Steps
1.  **Implement "Find Closest Facilities" feature**:
    - Add a new UI section in the location selection step for defining facility types.
    - Implement the logic to search for and add the nearest facilities to the map.
2.  Address the uBlock Origin conflict that blocks certain scripts.
3.  Investigate and resolve the deprecation warnings from the Google Maps API.

## Active Decisions and Considerations
*   The use of persistent colors for each location provides significant UX benefits and should be maintained as a core design principle.
*   The logic for calculating summary statistics (best/worst/average) is now aligned with user expectations and provides a more realistic travel time forecast for both outbound and return trips.
*   The `departureTime` for the Distance Matrix API must be set to the future. The current logic checks if the selected time has passed for today and sets it for tomorrow if needed. This is the correct and stable solution.
